
<nav id="otherNav" class="navbar navbar-default navbar-fixed-top affix">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a id="logoContainer" class="navbar-brand" href="<%= root_path %>#page-top">
        <img id="headerLogo" src="<%= image_url 'romano_logo.png' %>" alt="romano_logo"/>
      </a>
      <a class="navbar-brand" href="<%= root_path %>#page-top">Romano Capital</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
        <li>
          <a class="page-scroll collapse-the-nav" href="/">Home</a>
        </li>
        <li>
          <a class="page-scroll collapse-the-nav" href="/#about">About</a>
        </li>
        <li>
          <a class="page-scroll collapse-the-nav" href="/#contact">Contact</a>
        </li>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown">More Info<span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><a class="collapse-the-nav" href="<%= funds_path %>">Funds List</a></li>
            <li role="separator" class="divider"></li>
            <li><a class="collapse-the-nav" href="/#projects">Project Map</a></li>
            <li><a class="collapse-the-nav" href="<%= projects_path %>">Projects List</a></li>
          </ul>
        </li>
        <% if current_user.present? %>
          <li class="dropdown">
            <a class="dropdown-toggle" href="#" data-toggle="dropdown">My Account<span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li id="nav-name"><%= current_user.first_name %> <%= current_user.last_name %></li>
              <li><a class="collapse-the-nav" href="<%= member_home_path %>">My Homepage</a></li>
              <li role="separator" class="divider"></li>
              <li><%= link_to("Sign Out", destroy_user_session_path, method: :delete, class: "collapse-the-nav") %></li>
            </ul>
          </li>
        <% else %>
          <li class="<%= 'active' if current_page?(new_user_session_path) %>"><a id="login" class="collapse-the-nav" href="<%= new_user_session_path %>">Member Login</a></li>
        <% end %>
      </ul>
    </div>
    <!-- /.navbar-collapse -->
  </div>
  <!-- /.container-fluid -->
</nav>
